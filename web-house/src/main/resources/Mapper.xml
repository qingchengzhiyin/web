<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMapper">

<!--    <select id="getUserById" parameterType="int" resultType="com.entity.User">-->
<!--        SELECT * FROM users WHERE id = #{id}-->
<!--    </select>-->

    <!-- 其他查询和映射关系 -->
    <select id="getAllUsers" resultType="com.entity.User">
        SELECT * FROM user
    </select>

    <delete id="deleteUserById" parameterType="int">
        DELETE FROM user WHERE userId = #{id}
    </delete>

    <select id="getUserById" parameterType="int" resultType="com.entity.User">
        SELECT * FROM user WHERE userId = #{id}
    </select>

    <update id="updateUser" parameterType="com.entity.User">
        UPDATE user
        SET userName = #{username}, phoneNumber = #{phone}
        WHERE userId = #{id}
    </update>
    <insert id="addUser" parameterType="com.entity.User" >
        INSERT INTO user (userName, userPwd, phoneNumber) VALUES (#{username}, #{password}, #{phone})
    </insert>

</mapper>
